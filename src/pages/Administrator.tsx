import { useCallback, useEffect, useState } from "react";
import type { CSSProperties, ReactNode } from "react";
import { NavLink, useLocation, useNavigate } from "react-router-dom";
import { BarChart3, BriefcaseBusiness, Building2, ClipboardList, FileCheck2, FolderKanban, LayoutDashboard, LogOut, Menu, MessageSquare, RefreshCw, Settings, TrendingUp, UserCheck, Users, Wallet, X } from "lucide-react";
import "../styles/main.css";
const API="http://localhost/aapi-api/auth/admin/admin.php"; type R=Record<string,any>; type Section="dashboard"|"users"|"investors"|"projects"|"investments"|"requests"|"messages"|"documents";
const labels:Record<string,string>={brouillon:"مسودة",soumis:"مُرسل",en_etude:"قيد الدراسة",approuve:"مقبول",en_cours:"قيد التنفيذ",realise:"منجز",rejete:"مرفوض",archive:"مؤرشف",actif:"نشط",inactif:"غير نشط",suspendu:"موقوف",en_attente:"في الانتظار",valide:"مقبول",termine:"منتهي",annule:"ملغى",nouvelle:"جديد",acceptee:"مقبول",refusee:"مرفوض",terminee:"منتهٍ",lu:"مقروء",non_lu:"غير مقروء"};
const projectStatuses=["brouillon","soumis","en_etude","approuve","en_cours","realise","rejete","archive"],userStatuses=["actif","inactif","suspendu"],investmentStatuses=["en_attente","valide","en_cours","termine","annule"],requestStatuses=["nouvelle","en_cours","en_attente","acceptee","refusee","terminee"],documentStatuses=["en_attente","valide","rejete"];
const getUser=():R|null=>{try{const x=localStorage.getItem("aapi_user"),u=x?JSON.parse(x):null;return u&&typeof u==="object"?u:null}catch{return null}}; const text=(v:any)=>labels[String(v??"")]??String(v??"—").replaceAll("_"," "); const fmt=(v:any)=>Number(v??0).toLocaleString("fr-DZ"); const da=(v:any)=>`${fmt(v)} DA`; const person=(r:R)=>`${r.prenom??""} ${r.nom??""}`.trim()||r.email||"—";
function AdminNavbar({onToggle}:{onToggle:()=>void}){const nav=useNavigate(),u=getUser()??{};const items:Array<[string,string,ReactNode]>=[["/admin/dashboard","لوحة التحكم",<LayoutDashboard size={18}/>],["/admin/users","المستخدمون",<Users size={18}/>],["/admin/investors","المستثمرون",<UserCheck size={18}/>],["/admin/projects","المشاريع",<FolderKanban size={18}/>],["/admin/investments","الاستثمارات",<Wallet size={18}/>],["/admin/requests","الطلبات",<ClipboardList size={18}/>],["/admin/messages","الرسائل",<MessageSquare size={18}/>],["/admin/documents","الوثائق",<FileCheck2 size={18}/>]];const logout=()=>{localStorage.removeItem("aapi_user");nav("/login",{replace:true})};return <><div className="admin-mobile-header"><button className="admin-mobile-menu-button" onClick={onToggle}><Menu size={20}/></button><strong>AAPI</strong></div><aside className="admin-navbar"><div className="admin-navbar-brand"><button className="admin-navbar-brand-button" onClick={()=>nav("/admin/dashboard")}><span className="admin-navbar-logo">A</span><span className="admin-navbar-brand-text"><strong>AAPI</strong><small>الإدارة المركزية</small></span></button><button className="admin-navbar-mobile-close" onClick={onToggle}><X size={18}/></button></div><div className="admin-navbar-user"><span className="admin-navbar-user-avatar">{String(u.prenom??u.nom??"A").slice(0,2).toUpperCase()}</span><span className="admin-navbar-user-info"><strong>{`${u.prenom??""} ${u.nom??""}`.trim()||u.email||"Administrateur"}</strong><span>مسؤول النظام</span></span></div><nav className="admin-navbar-menu"><div className="admin-navbar-section"><div className="admin-navbar-section-title">الإدارة</div><div className="admin-navbar-section-items">{items.map(([to,n,icon])=><NavLink key={to} to={to} className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon">{icon}</span><span className="admin-nav-label">{n}</span></NavLink>)}</div></div><div className="admin-navbar-section"><div className="admin-navbar-section-title">النظام</div><NavLink to="/admin/settings" className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon"><Settings size={18}/></span><span className="admin-nav-label">الإعدادات</span></NavLink></div></nav><div className="admin-navbar-bottom"><button className="admin-navbar-bottom-button" onClick={()=>nav("/")}><Building2 size={17}/> الموقع العام</button><button className="admin-navbar-bottom-button admin-navbar-logout" onClick={logout}><LogOut size={17}/> تسجيل الخروج</button></div></aside></>}
function Stat({icon,title,value}:{icon:ReactNode;title:string;value:any}){return <div className="admin-kpi-card"><div className="admin-kpi-icon">{icon}</div><div className="admin-kpi-content"><span>{title}</span><strong>{typeof value==="number"?fmt(value):value}</strong></div><TrendingUp className="admin-kpi-arrow" size={17}/></div>}
function Progress({title,value,total}:{title:string;value:any;total:any}){const p=Number(total)?Math.min(100,Math.round(Number(value)/Number(total)*100)):0;return <div className="admin-progress-item"><div className="admin-progress-top"><span>{title}</span><strong>{p}%</strong></div><div className="admin-progress-bar"><span style={{"--progress":`${p}%`} as CSSProperties}/></div></div>}
function Dashboard({userId}:{userId:number}){const[data,setData]=useState<R>({}),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);try{const r=await fetch(`${API}?action=dashboard&user_id=${userId}`),j=await r.json();if(!j.success)throw Error(j.message||"تعذر تحميل لوحة التحكم");setData(j);setError("")}catch(e){setError(e instanceof Error?e.message:"حدث خطأ")}finally{setLoading(false)}},[userId]);useEffect(()=>{void load()},[load]);const s=data.stats??{},inv=s.investments??data.investments??{},projects=Array.isArray(data.projects)?data.projects:Array.isArray(data.recent_projects)?data.recent_projects:[],total=Number(s.projects_total??s.total_projects??0),approved=Number(s.projects_approuve??s.approuve??0),active=Number(s.projects_en_cours??s.en_cours??0),investors=Number(s.investors_total??s.investisseurs??0),approval=Number(s.projects_approved_percent??(total?approved/total*100:0)),activePct=Number(s.projects_active_percent??(total?active/total*100:0)),docPct=Number(s.documents_valid_percent??0);return <div className="admin-dashboard"><header className="admin-dashboard-header"><div className="admin-dashboard-header-content"><div className="admin-dashboard-welcome"><span className="admin-dashboard-eyebrow">AAPI / ADMIN</span><h1>لوحة التحكم</h1><p>نظرة شاملة على نشاط الوكالة والاستثمار.</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></div></header>{error&&<div className="admin-dashboard-error"><span>{error}</span><button onClick={()=>void load()}>إعادة المحاولة</button></div>}<main className="admin-dashboard-main"><div className="admin-kpi-grid"><Stat icon={<FolderKanban size={20}/>} title="إجمالي المشاريع" value={total}/><Stat icon={<Users size={20}/>} title="المستثمرون" value={investors}/><Stat icon={<Wallet size={20}/>} title="قيمة الاستثمارات" value={da(s.investments_total??inv.montant_total??0)}/><Stat icon={<BriefcaseBusiness size={20}/>} title="مناصب العمل" value={s.jobs_total??s.total_jobs??0}/></div><div className="admin-analytics-grid"><section className="admin-panel"><div className="admin-panel-header"><h2>مؤشرات المشاريع</h2><BarChart3 size={18}/></div><div className="admin-circular-stats">{[[approval,"المشاريع المقبولة"],[activePct,"المشاريع النشطة"],[docPct,"الوثائق السليمة"]].map(([p,l])=><div key={String(l)}><div className="admin-circular-progress" style={{"--progress":Math.max(0,Math.min(100,Number(p)))} as CSSProperties}><div className="admin-circular-inner">{Math.round(Number(p))}%</div></div><div className="admin-circular-info">{l}</div></div>)}</div></section><section className="admin-panel"><div className="admin-panel-header"><h2>حالة الاستثمارات</h2><Wallet size={18}/></div><div className="admin-progress-list"><Progress title="في الانتظار" value={inv.en_attente??0} total={inv.total??0}/><Progress title="مقبولة" value={inv.valide??0} total={inv.total??0}/><Progress title="قيد التنفيذ" value={inv.en_cours??0} total={inv.total??0}/></div></section></div><section className="admin-recent-projects"><header><h2>آخر المشاريع</h2></header><div className="admin-project-table-wrapper"><table className="admin-project-table"><thead><tr><th>المشروع</th><th>المستثمر</th><th>الولاية</th><th>الحالة</th><th>القيمة</th></tr></thead><tbody>{projects.slice(0,8).map((p:R,i:number)=><tr key={p.id??i}><td className="admin-project-name">{p.titre??p.nom??"مشروع"}</td><td>{p.investisseur??person(p)}</td><td>{p.wilaya??"—"}</td><td><span className={`admin-status-badge status-${p.statut}`}>{text(p.statut)}</span></td><td>{da(p.montant_investissement??p.montant??0)}</td></tr>)}</tbody></table>{projects.length===0&&<div className="admin-empty-state">لا توجد مشاريع حالياً.</div>}</div></section></main></div>}
function DataPage({section,userId}:{section:Exclude<Section,"dashboard">;userId:number}){const cfg:Record<Exclude<Section,"dashboard">,{title:string;subtitle:string;action:string}>={users:{title:"المستخدمون",subtitle:"إدارة حسابات مستخدمي المنصة",action:"users"},investors:{title:"المستثمرون",subtitle:"إدارة ملفات المستثمرين",action:"investors"},projects:{title:"المشاريع",subtitle:"متابعة ومراجعة مشاريع الاستثمار",action:"projects"},investments:{title:"الاستثمارات",subtitle:"متابعة عمليات الاستثمار",action:"investments"},requests:{title:"الطلبات",subtitle:"متابعة طلبات المستثمرين وتحديث حالتها",action:"requests"},messages:{title:"الرسائل",subtitle:"إدارة رسائل المستثمرين ومتابعة المقروء وغير المقروء",action:"messages"},documents:{title:"الوثائق",subtitle:"مراجعة وثائق المستثمرين والتحقق من حالتها",action:"documents"}},c=cfg[section];const[rows,setRows]=useState<R[]>([]),[stats,setStats]=useState<R>({}),[loading,setLoading]=useState(true),[error,setError]=useState(""),[search,setSearch]=useState(""),[filter,setFilter]=useState("all"),[saving,setSaving]=useState<number|null>(null);const load=useCallback(async()=>{setLoading(true);try{const q=new URLSearchParams({action:c.action,user_id:String(userId)});if(search)q.set("search",search);if(filter!=="all")q.set("statut",filter);const r=await fetch(`${API}?${q}`),j=await r.json();if(!j.success)throw Error(j.message||"تعذر تحميل البيانات");setRows(Array.isArray(j[section])?j[section]:[]);setStats(j.stats??{});setError("")}catch(e){setError(e instanceof Error?e.message:"حدث خطأ")}finally{setLoading(false)}},[c.action,filter,search,section,userId]);useEffect(()=>{void load()},[load]);const options=section==="users"||section==="investors"?userStatuses:section==="projects"?projectStatuses:section==="investments"?investmentStatuses:section==="requests"?requestStatuses:section==="documents"?documentStatuses:[];
const update=async(row:R,status:string)=>{const id=Number(row.id);if(!id)return;let action="";const body:R={user_id:userId};if(section==="users"){action="update_user_status";body.target_user_id=id}else if(section==="investors"){action="update_investor_status";body.target_user_id=id}else if(section==="projects"){action="update_project_status";body.project_id=id}else if(section==="investments"){action="update_investment_status";body.investment_id=id}else if(section==="requests"){action="update_request_status";body.request_id=id}else if(section==="documents"){action="update_document_status";body.document_id=id}else return;body.action=action;body.statut=status;setSaving(id);try{const r=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),j=await r.json();if(!j.success)throw Error(j.message||"تعذر تحديث الحالة");setRows(prev=>prev.map(x=>x.id===row.id?{...x,statut:status}:x));setError("")}catch(e){setError(e instanceof Error?e.message:"تعذر تحديث الحالة")}finally{setSaving(null)}};
const toggleMessage=async(row:R)=>{const id=Number(row.id);if(!id)return;const read=row.lu===1||row.lu===true||row.statut==="lu";const action=read?"mark_message_unread":"mark_message_read";setSaving(id);try{const r=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({user_id:userId,action,message_id:id})}),j=await r.json();if(!j.success)throw Error(j.message||"تعذر تحديث الرسالة");setRows(prev=>prev.map(x=>x.id===row.id?{...x,lu:read?0:1,statut:read?"non_lu":"lu"}:x));setError("")}catch(e){setError(e instanceof Error?e.message:"تعذر تحديث الرسالة")}finally{setSaving(null)}};
const keys=rows[0]?Object.keys(rows[0]).filter(k=>!["created_at","updated_at"].includes(k)).slice(0,6):[];return <div className="admin-dashboard"><header className="admin-dashboard-header"><div className="admin-dashboard-header-content"><div className="admin-dashboard-welcome"><span className="admin-dashboard-eyebrow">AAPI / ADMIN</span><h1>{c.title}</h1><p>{c.subtitle}</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></div></header>{error&&<div className="admin-dashboard-error"><span>{error}</span></div>}<main className="admin-dashboard-main"><section className="admin-panel"><div className="admin-panel-header"><h2>{c.title}</h2><span>{fmt(stats.total??rows.length)} عنصر</span></div><div className="admin-toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="بحث..."/><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">كل الحالات</option>{options.map(o=><option key={o} value={o}>{text(o)}</option>)}</select></div>{loading?<div className="admin-empty-state">جاري التحميل...</div>:rows.length===0?<div className="admin-empty-state">لا توجد بيانات.</div>:<div className="admin-project-table-wrapper"><table className="admin-project-table"><thead><tr>{keys.map(k=><th key={k}>{text(k)}</th>)}{options.length>0&&<th>الإجراء</th>}{section==="messages"&&<th>القراءة</th>}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id??i}>{keys.map(k=><td key={k}>{k==="statut"?<span className={`admin-status-badge status-${r[k]}`}>{text(r[k])}</span>:k.includes("montant")||k.includes("investissement")?da(r[k]):String(r[k]??"—")}</td>)}{options.length>0&&<td><select className="status-select" value={String(r.statut??"")} disabled={saving===Number(r.id)} onChange={e=>void update(r,e.target.value)}><option value="">—</option>{options.map(o=><option key={o} value={o}>{text(o)}</option>)}</select></td>}{section==="messages"&&<td><button className={`admin-read-toggle ${(r.lu===1||r.lu===true||r.statut==="lu")?"is-read":"is-unread"}`} disabled={saving===Number(r.id)} onClick={()=>void toggleMessage(r)}>{r.lu===1||r.lu===true||r.statut==="lu"?"تعيين كغير مقروء":"تعيين كمقروء"}</button></td>}</tr>)}</tbody></table></div>}</section></main></div>}
export default function Administrator(){const loc=useLocation(),path=loc.pathname;const[open,setOpen]=useState(false);const user=getUser(),userId=Number(user?.id??0),name=path.split("/")[2] as Section|undefined;const section:Section=name&&["users","investors","projects","investments","requests","messages","documents"].includes(name)?name:"dashboard";return <div className={`administrator-shell ${open?"admin-sidebar-open":""}`} dir="rtl"><AdminNavbar onToggle={()=>setOpen(v=>!v)}/>{open&&<button className="admin-navbar-overlay" aria-label="إغلاق القائمة" onClick={()=>setOpen(false)}/>}<div className="admin-main-content">{section==="dashboard"?<Dashboard userId={userId}/>:<DataPage section={section} userId={userId}/>}</div></div>}
