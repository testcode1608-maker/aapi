import { useCallback, useEffect, useState } from "react";
import type { CSSProperties, ReactNode } from "react";
import { NavLink, useLocation, useNavigate } from "react-router-dom";
import { Activity, BarChart3, Building2, CheckCircle2, ClipboardList, FileCheck2, FolderKanban, LayoutDashboard, LogOut, Menu, MessageSquare, RefreshCw, Settings, TrendingUp, UserCheck, Users, Wallet, X } from "lucide-react";
import "../styles/main.css";

const API = "http://localhost/aapi-api/auth/admin/admin.php";
type AnyRecord = Record<string, any>;
type Section = "dashboard" | "users" | "investors" | "projects" | "investments" | "requests";

const labels: Record<string, string> = { brouillon:"مسودة", soumis:"مُرسل", en_etude:"قيد الدراسة", approuve:"مقبول", en_cours:"قيد التنفيذ", realise:"منجز", rejete:"مرفوض", archive:"مؤرشف", actif:"نشط", inactif:"غير نشط", suspendu:"موقوف", en_attente:"في الانتظار", valide:"مقبول", termine:"منتهي", annule:"ملغى", nouvelle:"جديد", acceptee:"مقبول", refusee:"مرفوض", terminee:"منتهٍ" };
const projectStatuses = ["brouillon","soumis","en_etude","approuve","en_cours","realise","rejete","archive"];
const userStatuses = ["actif","inactif","suspendu"];
const investmentStatuses = ["en_attente","valide","en_cours","termine","annule"];

const getUser = (): AnyRecord | null => { try { const raw = localStorage.getItem("aapi_user"); const user = raw ? JSON.parse(raw) : null; return user && typeof user === "object" ? user : null; } catch { return null; } };
const text = (v: any) => labels[String(v ?? "")] ?? String(v ?? "—").replaceAll("_", " ");
const fmt = (v: any) => Number(v ?? 0).toLocaleString("fr-DZ");
const da = (v: any) => `${fmt(v)} DA`;

function AdminNavbar({ onToggle }: { onToggle: () => void }) {
  const nav = useNavigate(); const user = getUser() ?? {};
  const items: Array<[string,string,ReactNode]> = [["/admin/dashboard","لوحة التحكم",<LayoutDashboard size={18}/>],["/admin/users","المستخدمون",<Users size={18}/>],["/admin/investors","المستثمرون",<UserCheck size={18}/>],["/admin/projects","المشاريع",<FolderKanban size={18}/>],["/admin/investments","الاستثمارات",<Wallet size={18}/>],["/admin/requests","الطلبات",<ClipboardList size={18}/>],["/admin/messages","الرسائل",<MessageSquare size={18}/>],["/admin/documents","الوثائق",<FileCheck2 size={18}/>]];
  const logout = () => { localStorage.removeItem("aapi_user"); nav("/login", { replace:true }); };
  return <><div className="admin-mobile-header"><button className="admin-mobile-menu-button" onClick={onToggle}><Menu size={20}/></button><strong>AAPI</strong></div><aside className="admin-navbar"><div className="admin-navbar-brand"><button className="admin-navbar-brand-button" onClick={()=>nav("/admin/dashboard")}><span className="admin-navbar-logo">A</span><span className="admin-navbar-brand-text"><strong>AAPI</strong><small>الإدارة المركزية</small></span></button><button className="admin-navbar-mobile-close" onClick={onToggle}><X size={18}/></button></div><div className="admin-navbar-user"><span className="admin-navbar-user-avatar">{String(user.prenom ?? user.nom ?? "A").slice(0,2).toUpperCase()}</span><span className="admin-navbar-user-info"><strong>{`${user.prenom ?? ""} ${user.nom ?? ""}`.trim() || user.email || "Administrateur"}</strong><span>مسؤول النظام</span></span></div><nav className="admin-navbar-menu"><div className="admin-navbar-section"><div className="admin-navbar-section-title">الإدارة</div><div className="admin-navbar-section-items">{items.map(([to,name,icon])=><NavLink key={to} to={to} className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon">{icon}</span><span className="admin-nav-label">{name}</span></NavLink>)}</div></div><div className="admin-navbar-section"><div className="admin-navbar-section-title">النظام</div><div className="admin-navbar-section-items"><NavLink to="/admin/settings" className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon"><Settings size={18}/></span><span className="admin-nav-label">الإعدادات</span></NavLink></div></div></nav><div className="admin-navbar-bottom"><button className="admin-navbar-bottom-button" onClick={()=>nav("/")}><Building2 size={17}/> الموقع العام</button><button className="admin-navbar-bottom-button admin-navbar-logout" onClick={logout}><LogOut size={17}/> تسجيل الخروج</button></div></aside></>;
}

function Stat({ icon, title, value }: { icon: ReactNode; title: string; value: any }) { return <div className="admin-kpi-card"><div className="admin-kpi-icon">{icon}</div><div className="admin-kpi-content"><span>{title}</span><strong>{typeof value === "number" ? fmt(value) : value}</strong></div><TrendingUp className="admin-kpi-arrow" size={17}/></div>; }
function Progress({ title, value, total }: { title: string; value: any; total: any }) { const p = Number(total) ? Math.min(100, Math.round(Number(value)/Number(total)*100)) : 0; return <div className="admin-progress-item"><div className="admin-progress-top"><span>{title}</span><strong>{p}%</strong></div><div className="admin-progress-bar"><span style={{"--progress":`${p}%`} as CSSProperties}/></div></div>; }
function SelectStatus({ value, options, disabled, onChange }: { value: string; options: string[]; disabled?: boolean; onChange: (v:string)=>void }) { return <select className={`status-select status-${value}`} value={value ?? ""} disabled={disabled} onChange={e=>onChange(e.target.value)}>{options.map(s=><option key={s} value={s}>{text(s)}</option>)}</select>; }

function Dashboard({ userId }: { userId: number }) {
  const [data,setData]=useState<AnyRecord>({}); const [loading,setLoading]=useState(true); const [error,setError]=useState("");
  const load=useCallback(async()=>{setLoading(true);setError("");try{const r=await fetch(`${API}?action=dashboard&user_id=${userId}`);const j=await r.json();if(!j.success)throw new Error(j.message||"تعذر تحميل لوحة التحكم");setData(j);}catch(e){setError(e instanceof Error?e.message:"حدث خطأ");}finally{setLoading(false);}},[userId]);
  useEffect(()=>{void load();},[load]);
  const s=data.stats??{}; const inv=data.investments??{}; const projects=Array.isArray(data.projects)?data.projects:Array.isArray(data.recent_projects)?data.recent_projects:[]; const total=Number(s.projects_total??s.total_projects??s.total??0); const approved=Number(s.projects_approuve??s.approuve??0); const active=Number(s.projects_en_cours??s.en_cours??0); const investors=Number(s.investors_total??s.investisseurs??0); const rate=total?Math.round(approved/total*100):0;
  return <div className="admin-dashboard"><header className="admin-dashboard-header"><div className="admin-dashboard-header-content"><div className="admin-dashboard-welcome"><span className="admin-dashboard-eyebrow">AAPI / ADMIN</span><h1>لوحة التحكم</h1><p>نظرة شاملة على نشاط الوكالة والاستثمار.</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></div></header>{error&&<div className="admin-dashboard-error"><span>{error}</span><button onClick={()=>void load()}>إعادة المحاولة</button></div>}<main className="admin-dashboard-main"><div className="admin-kpi-grid"><Stat icon={<FolderKanban size={20}/>} title="إجمالي المشاريع" value={total}/><Stat icon={<Users size={20}/>} title="المستثمرون" value={investors}/><Stat icon={<Wallet size={20}/>} title="قيمة الاستثمارات" value={da(s.investments_total??inv.montant_total??0)}/><Stat icon={<BriefcaseBusiness size={20}/>} title="مناصب العمل" value={s.jobs_total??s.total_jobs??0}/></div><div className="admin-analytics-grid"><section className="admin-panel"><div className="admin-panel-header"><h2>مؤشرات المشاريع</h2><BarChart3 size={18}/></div><div className="admin-circular-stats"><div><div className="admin-circular-progress" style={{"--progress":rate} as CSSProperties}><div className="admin-circular-inner">{rate}%</div></div><div className="admin-circular-info">المشاريع المقبولة</div></div><div><div className="admin-circular-progress" style={{"--progress":total?Math.round(active/total*100):0} as CSSProperties}><div className="admin-circular-inner">{total?Math.round(active/total*100):0}%</div></div><div className="admin-circular-info">المشاريع النشطة</div></div><div><div className="admin-circular-progress" style={{"--progress":Number(s.documents_valid_percent??0)} as CSSProperties}><div className="admin-circular-inner">{Number(s.documents_valid_percent??0)}%</div></div><div className="admin-circular-info">الوثائق السليمة</div></div></div></section><section className="admin-panel"><div className="admin-panel-header"><h2>حالة الاستثمارات</h2><Wallet size={18}/></div><div className="admin-progress-list"><Progress title="في الانتظار" value={inv.en_attente??0} total={inv.total??0}/><Progress title="مقبولة" value={inv.valide??0} total={inv.total??0}/><Progress title="قيد التنفيذ" value={inv.en_cours??0} total={inv.total??0}/></div></section></div><section className="admin-recent-projects"><header><h2>آخر المشاريع</h2></header><div className="admin-project-table-wrapper"><table className="admin-project-table"><thead><tr><th>المشروع</th><th>المستثمر</th><th>الولاية</th><th>الحالة</th><th>القيمة</th></tr></thead><tbody>{projects.slice(0,8).map((p:AnyRecord,i:number)=><tr key={p.id??i}><td className="admin-project-name">{p.titre??p.nom??"مشروع"}</td><td>{p.investisseur??`${p.prenom??""} ${p.nom??""}`}</td><td>{p.wilaya??"—"}</td><td><span className={`admin-status-badge status-${p.statut}`}>{text(p.statut)}</span></td><td>{da(p.montant_investissement??p.montant??0)}</td></tr>)}</tbody></table>{projects.length===0&&<div className="admin-empty-state">لا توجد مشاريع حالياً.</div>}</div></section></main></div>;
}

function DataPage({ section, userId }: { section: Exclude<Section,"dashboard">; userId:number }) {
  const cfg: Record<Exclude<Section,"dashboard">,{title:string;subtitle:string;action:string}>={users:{title:"المستخدمون",subtitle:"إدارة حسابات مستخدمي المنصة",action:"users"},investors:{title:"المستثمرون",subtitle:"إدارة ملفات المستثمرين",action:"investors"},projects:{title:"المشاريع",subtitle:"متابعة ومراجعة مشاريع الاستثمار",action:"projects"},investments:{title:"الاستثمارات",subtitle:"متابعة عمليات الاستثمار",action:"investments"},requests:{title:"الطلبات",subtitle:"متابعة طلبات المستثمرين",action:"requests"}};
  const c=cfg[section]; const [rows,setRows]=useState<AnyRecord[]>([]); const [stats,setStats]=useState<AnyRecord>({}); const [loading,setLoading]=useState(true); const [error,setError]=useState(""); const [search,setSearch]=useState(""); const [filter,setFilter]=useState("all");
  const load=useCallback(async()=>{setLoading(true);setError("");try{const q=new URLSearchParams({action:c.action,user_id:String(userId)});if(search)q.set("search",search);if((section==="users"||section==="investors")&&filter!=="all")q.set("statut",filter);const r=await fetch(`${API}?${q}`);const j=await r.json();if(!j.success)throw new Error(j.message||"تعذر تحميل البيانات");setRows(Array.isArray(j[section])?j[section]:[]);setStats(j.stats??{});}catch(e){setError(e instanceof Error?e.message:"حدث خطأ");}finally{setLoading(false);}},[c.action,filter,search,section,userId]);
  useEffect(()=>{void load();},[load]);
  const update=async(row:AnyRecord,status:string)=>{let action="";const body:AnyRecord={user_id:userId,action,statut:status};if(section==="users"){body.action="update_user_status";body.target_user_id=row.id;}else if(section==="investors"){body.action="update_investor_status";body.target_user_id=row.id;}else if(section==="projects"){body.action="update_project_status";body.project_id=row.id;}else if(section==="investments"){body.action="update_investment_status";body.investment_id=row.id;}else return;try{const r=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const j=await r.json();if(!j.success)throw new Error(j.message||"فشل التحديث");await load();}catch(e){setError(e instanceof Error?e.message:"تعذر التحديث");}};
  const columns=section==="users"?["المستخدم","البريد الإلكتروني","الدور","الحالة"]:section==="investors"?["المستثمر","الشركة","الولاية","النوع","الحالة"]:section==="projects"?["المشروع","المستثمر","الولاية","الحالة","القيمة"]:section==="investments"?["المرجع","المستثمر","المشروع","المبلغ","الحالة"]:["الطلب","المستثمر","التاريخ","الحالة"];
  return <div className={`admin-${section}-page`}><header className="admin-dashboard-header"><div><span className="admin-section-kicker">AAPI / ADMIN</span><h1>{c.title}</h1><p>{c.subtitle}</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></header><main className={`admin-${section}-content`}><div className="admin-kpi-grid"><Stat icon={<Activity size={20}/>} title="الإجمالي" value={stats.total??rows.length}/><Stat icon={<CheckCircle2 size={20}/>} title="النشط / المقبول" value={stats.actifs??stats.approuve??stats.valide??0}/><Stat icon={<TrendingUp size={20}/>} title="قيد المعالجة" value={stats.en_cours??stats.soumis??stats.nouvelle??0}/><Stat icon={<BarChart3 size={20}/>} title="النسبة" value={`${stats.pourcentage_actifs??0}%`}/></div><div className="admin-project-filters"><div className="project-search"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="بحث..."/></div>{(section==="users"||section==="investors")&&<select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">كل الحالات</option>{userStatuses.map(s=><option key={s} value={s}>{text(s)}</option>)}</select>}</div>{error&&<div className="admin-page-error"><span>{error}</span><button onClick={()=>void load()}>إعادة المحاولة</button></div>}<section className="admin-project-table-card"><div className="table-card-header"><h2>{c.title}</h2><span>{rows.length} سجل</span></div><div className="admin-project-table-wrapper">{loading?<div className="admin-empty-state"><RefreshCw className="spin" size={22}/> جارٍ التحميل...</div>:rows.length===0?<div className="admin-empty-state">لا توجد بيانات مطابقة.</div>:<table className="admin-project-table"><thead><tr>{columns.map(col=><th key={col}>{col}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id??i}>{section==="users"&&<><td className="admin-project-name">{r.prenom} {r.nom}</td><td>{r.email}</td><td>{r.role}</td><td><SelectStatus value={r.statut} options={userStatuses} disabled={r.role==="admin"} onChange={v=>void update(r,v)}/></td></>}{section==="investors"&&<><td className="admin-project-name">{r.prenom} {r.nom}</td><td>{r.nom_entreprise??"—"}</td><td>{r.wilaya??"—"}</td><td>{text(r.type_investisseur)}</td><td><SelectStatus value={r.statut} options={userStatuses} onChange={v=>void update(r,v)}/></td></>}{section==="projects"&&<><td className="admin-project-name">{r.titre??r.nom??"مشروع"}</td><td>{r.investisseur??`${r.prenom??""} ${r.nom??""}`}</td><td>{r.wilaya??"—"}</td><td><SelectStatus value={r.statut} options={projectStatuses} onChange={v=>void update(r,v)}/></td><td>{da(r.montant_investissement??r.montant??0)}</td></>}{section==="investments"&&<><td>{r.reference??`#${r.id}`}</td><td>{r.prenom} {r.nom}</td><td>{r.titre_projet??"—"}</td><td>{da(r.montant)}</td><td><SelectStatus value={r.statut} options={investmentStatuses} onChange={v=>void update(r,v)}/></td></>}{section==="requests"&&<><td className="admin-project-name">{r.objet??r.type_demande??"طلب"}</td><td>{r.prenom} {r.nom}</td><td>{r.created_at??r.date_creation??"—"}</td><td><span className={`admin-status-badge status-${r.statut}`}>{text(r.statut)}</span></td></>}</tr>)}</tbody></table>}</div></section></main></div>;
}

export default function Administrator(){const location=useLocation();const [mobile,setMobile]=useState(false);const user=getUser();if(!user||String(user.role).toLowerCase()!=="admin")return null;const id=Number(user.id??0);const p=location.pathname;const section:Section=p.includes("/users")?"users":p.includes("/investors")?"investors":p.includes("/projects")?"projects":p.includes("/investments")?"investments":p.includes("/requests")?"requests":"dashboard";return <div dir="rtl" className={`administrator-shell ${mobile?"admin-mobile-open":""}`}><AdminNavbar onToggle={()=>setMobile(v=>!v)}/>{mobile&&<button className="admin-navbar-overlay" onClick={()=>setMobile(false)} aria-label="إغلاق القائمة"/>}{section==="dashboard"?<Dashboard userId={id}/>:<DataPage section={section} userId={id}/>}</div>;}
