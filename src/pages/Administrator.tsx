import { useCallback, useEffect, useState } from "react";
import type { CSSProperties, ReactNode } from "react";
import { NavLink, useLocation, useNavigate } from "react-router-dom";
import { BarChart3, BriefcaseBusiness, Building2, ClipboardList, FileCheck2, FolderKanban, LayoutDashboard, LogOut, Menu, MessageSquare, RefreshCw, Settings, TrendingUp, UserCheck, Users, Wallet, X } from "lucide-react";
import "../styles/main.css";

const API = "http://localhost/aapi-api/auth/admin/admin.php";
type AnyRecord = Record<string, any>;
type Section = "dashboard" | "users" | "investors" | "projects" | "investments" | "requests" | "messages" | "documents";
const labels: Record<string,string>={brouillon:"مسودة",soumis:"مُرسل",en_etude:"قيد الدراسة",approuve:"مقبول",en_cours:"قيد التنفيذ",realise:"منجز",rejete:"مرفوض",archive:"مؤرشف",actif:"نشط",inactif:"غير نشط",suspendu:"موقوف",en_attente:"في الانتظار",valide:"مقبول",termine:"منتهي",annule:"ملغى",nouvelle:"جديد",acceptee:"مقبول",refusee:"مرفوض",terminee:"منتهٍ",lu:"مقروء",non_lu:"غير مقروء",identite:"وثيقة هوية",registre_commerce:"السجل التجاري",document_fiscal:"وثيقة جبائية",business_plan:"دراسة جدوى",contrat:"عقد",attestation:"شهادة",certificat:"شهادة",autre:"أخرى",personne_physique:"شخص طبيعي",personne_morale:"شخص معنوي",investisseur_etranger:"مستثمر أجنبي"};
const projectStatuses=["brouillon","soumis","en_etude","approuve","en_cours","realise","rejete","archive"],userStatuses=["actif","inactif","suspendu"],investmentStatuses=["en_attente","valide","en_cours","termine","annule"],requestStatuses=["nouvelle","en_cours","en_attente","acceptee","refusee","terminee"],documentStatuses=["en_attente","valide","rejete"];
const getUser=():AnyRecord|null=>{try{const r=localStorage.getItem("aapi_user"),u=r?JSON.parse(r):null;return u&&typeof u==="object"?u:null}catch{return null}};
const text=(v:any)=>labels[String(v??"")]??String(v??"—").replaceAll("_"," "); const fmt=(v:any)=>Number(v??0).toLocaleString("fr-DZ"); const da=(v:any)=>`${fmt(v)} DA`; const person=(r:AnyRecord)=>`${r.prenom??""} ${r.nom??""}`.trim()||r.email||"—";
function AdminNavbar({onToggle}:{onToggle:()=>void}){const nav=useNavigate(),user=getUser()??{};const items:Array<[string,string,ReactNode]>=[["/admin/dashboard","لوحة التحكم",<LayoutDashboard size={18}/>],["/admin/users","المستخدمون",<Users size={18}/>],["/admin/investors","المستثمرون",<UserCheck size={18}/>],["/admin/projects","المشاريع",<FolderKanban size={18}/>],["/admin/investments","الاستثمارات",<Wallet size={18}/>],["/admin/requests","الطلبات",<ClipboardList size={18}/>],["/admin/messages","الرسائل",<MessageSquare size={18}/>],["/admin/documents","الوثائق",<FileCheck2 size={18}/>]];const logout=()=>{localStorage.removeItem("aapi_user");nav("/login",{replace:true})};return <><div className="admin-mobile-header"><button className="admin-mobile-menu-button" onClick={onToggle}><Menu size={20}/></button><strong>AAPI</strong></div><aside className="admin-navbar"><div className="admin-navbar-brand"><button className="admin-navbar-brand-button" onClick={()=>nav("/admin/dashboard")}><span className="admin-navbar-logo">A</span><span className="admin-navbar-brand-text"><strong>AAPI</strong><small>الإدارة المركزية</small></span></button><button className="admin-navbar-mobile-close" onClick={onToggle}><X size={18}/></button></div><div className="admin-navbar-user"><span className="admin-navbar-user-avatar">{String(user.prenom??user.nom??"A").slice(0,2).toUpperCase()}</span><span className="admin-navbar-user-info"><strong>{`${user.prenom??""} ${user.nom??""}`.trim()||user.email||"Administrateur"}</strong><span>مسؤول النظام</span></span></div><nav className="admin-navbar-menu"><div className="admin-navbar-section"><div className="admin-navbar-section-title">الإدارة</div><div className="admin-navbar-section-items">{items.map(([to,name,icon])=><NavLink key={to} to={to} className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon">{icon}</span><span className="admin-nav-label">{name}</span></NavLink>)}</div></div><div className="admin-navbar-section"><div className="admin-navbar-section-title">النظام</div><div className="admin-navbar-section-items"><NavLink to="/admin/settings" className={({isActive})=>`admin-nav-link${isActive?" active":""}`}><span className="admin-nav-icon"><Settings size={18}/></span><span className="admin-nav-label">الإعدادات</span></NavLink></div></div></nav><div className="admin-navbar-bottom"><button className="admin-navbar-bottom-button" onClick={()=>nav("/")}><Building2 size={17}/> الموقع العام</button><button className="admin-navbar-bottom-button admin-navbar-logout" onClick={logout}><LogOut size={17}/> تسجيل الخروج</button></div></aside></>}
function Stat({icon,title,value}:{icon:ReactNode;title:string;value:any}){return <div className="admin-kpi-card"><div className="admin-kpi-icon">{icon}</div><div className="admin-kpi-content"><span>{title}</span><strong>{typeof value==="number"?fmt(value):value}</strong></div><TrendingUp className="admin-kpi-arrow" size={17}/></div>}
function Progress({title,value,total}:{title:string;value:any;total:any}){const p=Number(total)?Math.min(100,Math.round(Number(value)/Number(total)*100)):0;return <div className="admin-progress-item"><div className="admin-progress-top"><span>{title}</span><strong>{p}%</strong></div><div className="admin-progress-bar"><span style={{"--progress":`${p}%`} as CSSProperties}/></div></div>}
function Dashboard({userId}:{userId:number}){const[data,setData]=useState<AnyRecord>({}),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{const r=await fetch(`${API}?action=dashboard&user_id=${userId}`),j=await r.json();if(!j.success)throw new Error(j.message||"تعذر تحميل لوحة التحكم");setData(j)}catch(e){setError(e instanceof Error?e.message:"حدث خطأ")}finally{setLoading(false)}},[userId]);useEffect(()=>{void load()},[load]);const s=data.stats??{},inv=s.investments??data.investments??{},projects=Array.isArray(data.projects)?data.projects:Array.isArray(data.recent_projects)?data.recent_projects:[],total=Number(s.projects_total??s.total_projects??0),approved=Number(s.projects_approuve??s.approuve??0),active=Number(s.projects_en_cours??s.en_cours??0),investors=Number(s.investors_total??s.investisseurs??0),approval=Number(s.projects_approved_percent??(total?approved/total*100:0)),activePct=Number(s.projects_active_percent??(total?active/total*100:0)),docPct=Number(s.documents_valid_percent??0);return <div className="admin-dashboard"><header className="admin-dashboard-header"><div className="admin-dashboard-header-content"><div className="admin-dashboard-welcome"><span className="admin-dashboard-eyebrow">AAPI / ADMIN</span><h1>لوحة التحكم</h1><p>نظرة شاملة على نشاط الوكالة والاستثمار.</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></div></header>{error&&<div className="admin-dashboard-error"><span>{error}</span><button onClick={()=>void load()}>إعادة المحاولة</button></div>}<main className="admin-dashboard-main"><div className="admin-kpi-grid"><Stat icon={<FolderKanban size={20}/>} title="إجمالي المشاريع" value={total}/><Stat icon={<Users size={20}/>} title="المستثمرون" value={investors}/><Stat icon={<Wallet size={20}/>} title="قيمة الاستثمارات" value={da(s.investments_total??inv.montant_total??0)}/><Stat icon={<BriefcaseBusiness size={20}/>} title="مناصب العمل" value={s.jobs_total??s.total_jobs??0}/></div><div className="admin-analytics-grid"><section className="admin-panel"><div className="admin-panel-header"><h2>مؤشرات المشاريع</h2><BarChart3 size={18}/></div><div className="admin-circular-stats">{[[approval,"المشاريع المقبولة"],[activePct,"المشاريع النشطة"],[docPct,"الوثائق السليمة"]].map(([p,label])=><div key={String(label)}><div className="admin-circular-progress" style={{"--progress":Math.max(0,Math.min(100,Number(p)))} as CSSProperties}><div className="admin-circular-inner">{Math.round(Number(p))}%</div></div><div className="admin-circular-info">{label}</div></div>)}</div></section><section className="admin-panel"><div className="admin-panel-header"><h2>حالة الاستثمارات</h2><Wallet size={18}/></div><div className="admin-progress-list"><Progress title="في الانتظار" value={inv.en_attente??0} total={inv.total??0}/><Progress title="مقبولة" value={inv.valide??0} total={inv.total??0}/><Progress title="قيد التنفيذ" value={inv.en_cours??0} total={inv.total??0}/></div></section></div><section className="admin-recent-projects"><header><h2>آخر المشاريع</h2></header><div className="admin-project-table-wrapper"><table className="admin-project-table"><thead><tr><th>المشروع</th><th>المستثمر</th><th>الولاية</th><th>الحالة</th><th>القيمة</th></tr></thead><tbody>{projects.slice(0,8).map((p:AnyRecord,i:number)=><tr key={p.id??i}><td className="admin-project-name">{p.titre??p.nom??"مشروع"}</td><td>{p.investisseur??person(p)}</td><td>{p.wilaya??"—"}</td><td><span className={`admin-status-badge status-${p.statut}`}>{text(p.statut)}</span></td><td>{da(p.montant_investissement??p.montant??0)}</td></tr>)}</tbody></table>{projects.length===0&&<div className="admin-empty-state">لا توجد مشاريع حالياً.</div>}</div></section></main></div>}
function DataPage({section,userId}:{section:Exclude<Section,"dashboard">;userId:number}){const cfg:Record<Exclude<Section,"dashboard">,{title:string;subtitle:string;action:string}>={users:{title:"المستخدمون",subtitle:"إدارة حسابات مستخدمي المنصة",action:"users"},investors:{title:"المستثمرون",subtitle:"إدارة ملفات المستثمرين",action:"investors"},projects:{title:"المشاريع",subtitle:"متابعة ومراجعة مشاريع الاستثمار",action:"projects"},investments:{title:"الاستثمارات",subtitle:"متابعة عمليات الاستثمار",action:"investments"},requests:{title:"الطلبات",subtitle:"متابعة طلبات المستثمرين وتحديث حالتها",action:"requests"},messages:{title:"الرسائل",subtitle:"إدارة رسائل المستثمرين ومتابعة المقروء وغير المقروء",action:"messages"},documents:{title:"الوثائق",subtitle:"مراجعة وثائق المستثمرين والتحقق من حالتها",action:"documents"}},c=cfg[section];const[rows,setRows]=useState<AnyRecord[]>([]),[stats,setStats]=useState<AnyRecord>({}),[loading,setLoading]=useState(true),[error,setError]=useState(""),[search,setSearch]=useState(""),[filter,setFilter]=useState("all");const load=useCallback(async()=>{setLoading(true);setError("");try{const q=new URLSearchParams({action:c.action,user_id:String(userId)});if(search)q.set("search",search);if(filter!=="all")q.set("statut",filter);const r=await fetch(`${API}?${q}`),j=await r.json();if(!j.success)throw new Error(j.message||"تعذر تحميل البيانات");const key=section;setRows(Array.isArray(j[key])?j[key]:[]);setStats(j.stats??{})}catch(e){setError(e instanceof Error?e.message:"حدث خطأ")}finally{setLoading(false)}},[c.action,filter,search,section,userId]);useEffect(()=>{void load()},[load]);const options=section==="users"||section==="investors"?userStatuses:section==="projects"?projectStatuses:section==="investments"?investmentStatuses:section==="requests"?requestStatuses:section==="documents"?documentStatuses:[];return <div className="admin-dashboard"><header className="admin-dashboard-header"><div className="admin-dashboard-header-content"><div className="admin-dashboard-welcome"><span className="admin-dashboard-eyebrow">AAPI / ADMIN</span><h1>{c.title}</h1><p>{c.subtitle}</p></div><button className="admin-refresh-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> تحديث</button></div></header>{error&&<div className="admin-dashboard-error">{error}</div>}<main className="admin-dashboard-main"><section className="admin-panel"><div className="admin-panel-header"><h2>{c.title}</h2><span>{fmt(stats.total??rows.length)} عنصر</span></div><div className="admin-toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="بحث..."/><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">كل الحالات</option>{options.map(o=><option key={o} value={o}>{text(o)}</option>)}</select></div>{loading?<div className="admin-empty-state">جاري التحميل...</div>:rows.length===0?<div className="admin-empty-state">لا توجد بيانات.</div>:<div className="admin-project-table-wrapper"><table className="admin-project-table"><thead><tr>{Object.keys(rows[0]).slice(0,6).map(k=><th key={k}>{text(k)}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id??i}>{Object.entries(r).slice(0,6).map(([k,v])=><td key={k}>{k==="statut"?<span className={`admin-status-badge status-${v}`}>{text(v)}</span>:String(v??"—")}</td>)}</tr>)}</tbody></table></div>}</section></main></div>}
export default function Administrator(){const loc=useLocation(),[mobileOpen,setMobileOpen]=useState(false),user=getUser(),section=(loc.pathname.split("/")[2]||"dashboard") as Section;const userId=Number(user?.id??0);if(!userId)return <div className="admin-empty-state">يجب تسجيل الدخول كمسؤول.</div>;return <div className={`admin-layout${mobileOpen?" admin-mobile-open":""}`} dir="rtl"><AdminNavbar onToggle={()=>setMobileOpen(v=>!v)}/><div className="admin-main-content">{section==="dashboard"?<Dashboard userId={userId}/>:<DataPage section={section as Exclude<Section,"dashboard">} userId={userId}/>}</div></div>}
